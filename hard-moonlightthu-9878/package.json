{
	"name": "hard-moonlightthu-9878",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"build": "mkdir -p dist && cp public/index.html dist/ && cp style.css dist/ && cp openai-slave.mjs dist/ && cp re.mjs dist/"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.7.5",
		"vitest": "~3.0.7",
		"wrangler": "^4.13.0"
	}
}
