{
    "name": "scrimba3-root", 
    "version": "1.0.0",    
    "private": true,       
    "dependencies": {
      "openai": "^4.96.0"
    },
    "devDependencies": { 
      "@cloudflare/vitest-pool-workers": "^0.7.5",
      "vitest": "~3.0.7",
      "wrangler": "^4.13.0"
    },
    "scripts": {
      
      "build": "mkdir -p dist && cp index.html dist/ && cp style.css dist/ && cp openai-slave.mjs dist/ && cp hard-moonlightthu-9878/src/index.js dist/ && cp dates.mjs dist/ && cp images/COKECOIN.jpg dist/ && cp images/add.svg dist/ && cp images/loader.svg dist/"
      
    }
  }